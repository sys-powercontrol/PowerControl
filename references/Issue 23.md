# Issue 23: Componente de Exportação Reutilizável

## Descrição
Criar um botão universal para exportação de dados em PDF e Excel.

## Requisitos
- Criar o componente `src/components/ExportButton.tsx`.
- Utilizar as bibliotecas `jspdf`, `jspdf-autotable` e `xlsx`.
- Propriedades: `data` (array), `filename` (string), `format` ('pdf' | 'xlsx').
- Estilo padronizado com os botões do sistema.
